# R7-Testovarka — инструкции для Claude Code

## Структура проекта

```
E:\R7Manager
├── r7_Testovarka.py        # Основной файл программы (~3700+ строк)
├── requirements.txt        # Зависимости
├── Distributives/          # Папка с дистрибутивами (.msi, .exe)
├── performance_full_.json  # Результаты тестов
├── batch_summary_.html     # Сводные отчёты Batch-режима
├── comparison_report_*.html # Отчёты сравнения версий
├── hashes.json             # Эталонные хеши (создаётся пользователем)
└── last_test_params.json   # Последние настройки тестов
```

## Основные классы и методы

### Класс R7Testovarka

| Метод | Описание |
|-------|----------|
| `__init__` | Инициализация, загрузка настроек, создание UI |
| `setup_ui` | Создание главного окна и вкладок |
| `_build_versions_tab` | Вкладка управления версиями |
| `_build_perf_tab` | Вкладка тестирования производительности |

### Управление версиями

| Метод | Описание |
|-------|----------|
| `detect_current_version` | Определение установленной версии через реестр |
| `refresh_distributives` | Сканирование папки Distributives |
| `install_version` | Установка дистрибутива (тихий режим) |
| `uninstall_current_version` | Удаление текущей версии |
| `check_hashes` | Проверка MD5/SHA256 дистрибутивов |
| `_hash_worker` | Фоновое вычисление хешей |
| `_show_hash_results` | Окно результатов проверки хешей |

### Тестирование производительности

| Метод | Описание |
|-------|----------|
| `run_full_benchmark` | Запуск полного теста (13 операций) |
| `run_quick_benchmark` | Быстрый тест (запуск + RAM/CPU) |
| `_spreadsheet_worker(self, enabled_tests=None, test_runs=None)` | Основной рабочий поток стресс-теста. `enabled_tests` — множество имён тестов к выполнению (`None` → все); `test_runs` — словарь имя_теста → число прогонов, снимок `self.test_runs`, снятый в главном потоке (`None` → пусто, используется дефолт 3). Обновляет `self.status_var` перед каждой операцией (текущее действие, прогресс, прошедшее время) и после завершения цикла. |
| `_bench_launch` | Замер времени запуска Р7-Офис |
| `_bench_text_input` | Тест ввода текста (10000 символов) |
| `_bench_memory` | Замер потребления RAM |
| `_bench_cpu` | Замер загрузки CPU |
| `_bench_close` | Закрытие Р7-Офис |
| `_get_r7_processes(log_cb=None)` | Поиск процессов Р7-Офис по psutil (editors_helper, desktopeditors, r7, р7, x2t — конвертер документов); кэширует PID; список обновляется перед каждым отдельным замером (не один раз за тест), чтобы ловить x2t, запущенный в середине теста; `log_cb` по умолчанию `self.add_test_log` |
| `_sample_r7_resources(procs)` | Снимает RAM (МБ), CPU (сырое и нормализованное делением на `psutil.cpu_count()`), число потоков и аптайм по списку процессов; общий метод для `_spreadsheet_worker` и `_batch_run_single_version` |
| `_log_resources(sample, log_cb=None)` | Форматированный вывод замера в лог с цветовым индикатором нагрузки CPU: 🟢 <50% (норм.), 🟡 50–79.9%, 🔴 ≥80% |
| `run_test_with_runs(name, func, runs)` | Локальная функция внутри `_spreadsheet_worker`. Выполняет `func` `runs` раз подряд, логируя каждый прогон, и добавляет в `results` один усреднённый результат (замер RAM/CPU/потоков/аптайма снимается один раз, после последнего прогона — не на каждый прогон). Если тест не в `enabled_tests`, ничего не делает. |
| `_win_title_contains(*substrings)` | Возвращает `True`, если заголовок любого видимого top-level окна содержит любую из переданных подстрок (без учёта регистра) |
| `_cleanup_x2t_temp_pdfs(log_cb=None)` | Удаляет оставшиеся временные `temp_export_x2t_*.pdf` из `%TEMP%` после теста «Сохранение в PDF»; безопасно вызывать, даже если `save_as_pdf` не запускался или упал на середине |
| `_apply_dark_theme` | Настраивает тёмную тему приложения через `ttk.Style` |
| `_set_perf_progress(done, total)` | Обновляет прогресс-бар вкладки «Производительность» (0–100%); безопасен для вызова даже до создания виджета |

Настройки выбора тестов и числа прогонов сохраняются в `selected_tests.json` в формате `{"<имя теста>": {"enabled": bool, "runs": int}}`. `_load_test_selection` принимает и старый формат (`{"<имя теста>": bool}`) и автоматически обновляет его в памяти до нового при загрузке — файлы, сохранённые более ранними версиями приложения, продолжают работать без миграции.

### Batch-режим и сравнение

| Метод | Описание |
|-------|----------|
| `compare_versions` | Сравнение результатов разных версий |
| `_show_batch_config_dialog` | Диалог настройки Batch-режима |
| `_batch_worker` | Фоновый поток Batch-режима |
| `_generate_batch_summary_html` | Сводный отчёт Batch-режима |

### Тестирование файлов

| Метод | Описание |
|-------|----------|
| `compare_file_sizes` | Диалог генерации тестовых файлов |
| `_run_custom_file_test` | Тестирование выбранного файла |
| `_generate_custom_test_file` | Создание тестового .xlsx файла |

### Управление окном обновления

| Метод | Описание |
|-------|----------|
| `_close_update_dialog_if_exists(log_cb, search_timeout)` | Ищет и закрывает диалог обновления через Win32-сообщения (без pyautogui). Сканирует минимум один раз, даже при `search_timeout=0`. Из последовательности открытия файла вызывать с `search_timeout=0` (один проход, ~30 мс) — иначе ожидание попадает в замер; из монитора — `search_timeout=1`. |
| `_monitor_update_dialog(stop_event, log_cb, interval)` | Фоновый поток-монитор: вызывает `_close_update_dialog_if_exists` каждые `interval` секунд (по умолчанию 2). Останавливается через `stop_event.set()`. Запускается в `_spreadsheet_worker` и `_batch_run_single_version` сразу после запуска Р7-Офис. |

### Замер отдельной операции

Формула: `elapsed = момент_простоя_Р7 − старт − self._paced_total`. Пороги — константы класса `OP_*`.

| Метод | Описание |
|-------|----------|
| `_pace(seconds)` | Преднамеренная пауза внутри операции; копится в `_paced_total` и вычитается из замера |
| `_wait_for_window_title(substrings, timeout)` | Ждёт окно с заголовком. Это реакция Р7, поэтому НЕ вычитается; вызывающий код сам добавляет к `_paced_total` безрезультатное ожидание |
| `_wait_operation_done(hwnd, log_cb, start_grace)` | Ждёт, пока Р7 освободится. Занят = окно не прокачивает очередь ИЛИ CPU процессов Р7 выше `OP_IDLE_CPU_PCT` ИЛИ жив x2t. Возвращает `(момент, статус)`, где момент — НАЧАЛО простоя, поэтому окно подтверждения не попадает в замер |

Статусы: `ok`, `below_floor` (Р7 не стал занят за `start_grace` — операция быстрее порога измерения, в отчёте помечается «<порога»), `timeout`.

Экспорт в PDF выставляет `self._op_start_grace = OP_PDF_GRACE_SEC` перед отправкой клавиш: x2t стартует не сразу после Enter, и при обычном окне старта операция помечалась бы `below_floor`. Ожидание старта в замер не попадает — оно стоит только времени прогона.

Правки в тест-функциях обязательно зеркалить в `_batch_run_single_version`: места и величины пауз должны совпадать, иначе Batch и вкладка «Производительность» дадут несравнимые цифры.

### Определение готовности документа

Замер «Открытие файла» заканчивается, когда `_wait_until_r7_ready` подтвердит готовность. Пороги — константы класса `READY_*`, общие для одиночного теста, теста своего файла и Batch-режима (прежние две копии ожидания разъехались: `STABLE_SECS` 10 против 8).

| Метод | Описание |
|-------|----------|
| `_window_responsive(hwnd, timeout_ms)` | `SendMessageTimeout(WM_NULL)` — возвращается ровно тогда, когда окно прокачало очередь сообщений. Прямое измерение занятости UI-потока |
| `_wait_until_r7_ready(hwnd, timeout, log_cb)` | Ждёт совпадения двух признаков: окно отзывчиво И процессы Р7 простаивают `READY_IDLE_SAMPLES` замеров подряд при отсутствии живого `x2t`. `hwnd` принимает дескриптор либо функцию его поиска (тогда устаревший дескриптор перерешивается). Возвращает `False` при таймауте и при исчезновении всех процессов Р7 |

Почему нужны оба признака: Р7 грузит данные в фоновом потоке и остаётся отзывчивым, поэтому одна отзывчивость срабатывает слишком рано; один простой CPU может совпасть с зависшим окном. Отдельно нужен контроль `x2t` — конвертер стартует уже после появления окна редактора, и пауза перед его запуском иначе читается как готовность.

Что делать, если открытие больших файлов стало определяться преждевременно: поднимать `READY_IDLE_SAMPLES`, не трогая `READY_POLL_SEC`.

### Тёмная тема

Словарь `COLORS` в начале файла — единственный источник цветов тёмной темы; `_apply_dark_theme` применяет его через `ttk.Style`. Тема `ttk` выбрана `clam` намеренно: нативные темы Windows (`vista`/`winnative`) рисуют часть виджетов средствами ОС и игнорируют цветовые переопределения `ttk.Style`, тогда как `clam` — собственный рендерер Tk с полной поддержкой кастомизации цвета. Главное окно сохраняет нативные заголовок и рамку ОС (`overrideredirect` не используется) — требование «кастомной шапки» реализовано добавленными виджетами-заголовком внутри содержимого окна, под нативным заголовком; это осознанный выбор в пользу избежания рисков самодельного безрамочного окна (перетаскивание, сворачивание/восстановление, поведение при DPI/snap), а не недосмотр.

### HTML-отчёты и PDF

PDF-экспорт реализован через браузер: кнопка `📄 Сохранить как PDF` вызывает `window.print()`. Reportlab не используется.

| Метод | Описание |
|-------|----------|
| `_generate_html_report(...)` | Основной отчёт производительности. Содержит кнопку печати и `@media print`. |
| `_generate_batch_summary_html(...)` | Сводный отчёт Batch-режима. Содержит кнопку печати и `@media print`. |
| `_generate_comparison_html(...)` | Отчёт сравнения версий. Содержит кнопку печати и `@media print`. |

## Вспомогательные функции окон

| Функция | Описание |
|---------|----------|
| `wait_for_window` | Ожидание появления окна по заголовку |
| `focus_window` | Активация окна Р7-Офис |
| `maximize_window` | Разворачивание окна на весь экран |
| `find_r7_window` | Поиск окна Р7-Офис |

## Правила работы с кодом

### 1. Импорты
- Стандартные библиотеки: `os`, `sys`, `time`, `threading`, `json`, `csv`, `hashlib`, `subprocess`, `winreg`, `shutil`, `re`, `tempfile`, `pathlib`, `datetime`, `tkinter`, `webbrowser`
- Сторонние: `pyautogui`, `psutil`, `pyperclip`, `openpyxl`, `win32gui`, `win32con`

### 2. Координаты и разрешение экрана
- НЕ использовать жёсткие координаты (например `pyautogui.click(x=800, y=400)`)
- Использовать `pyautogui.click(button='right')` без координат, или клик по центру найденного окна
- Навигацию по листам делать через горячие клавиши (`Ctrl+PageUp`, `Ctrl+PageDown`)

### 3. Замеры времени
- Не включать `time.sleep()` внутрь измеряемых функций. Там, где Р7 не успевает за
  клавиатурой (меню, буфер обмена), использовать `self._pace(sec)` — она копит время
  в `_paced_total`, и оно вычитается из результата
- `pyautogui.PAUSE = 0` выставлен на уровне модуля и `interval` в `hotkey` не передаётся:
  умолчание 0.1 сек добавлялось после каждого вызова и составляло весь результат замера
- Секундомер операции останавливать по `_wait_operation_done`, а не на последнем нажатии:
  pyautogui возвращается сразу после отправки клавиш, не дожидаясь обработки в Р7
- Паузы вне замера выносить в `post_action_delay()` после `measure()`
- Для ожидания загрузки данных использовать `_wait_until_r7_ready` (отзывчивость окна + простой CPU).
  Прежний зонд через `Ctrl+End` удалён: `keybd_event` не дожидается обработки, поэтому его
  длительность была константой и о состоянии Р7 не говорила ничего
- Подготовку окна (разворот, фокус, снятие диалога обновления) засекать отдельно и вычитать
  из `open_elapsed` — она не относится к скорости открытия файла

### 4. Обработка ошибок
- Все вызовы `win32gui.SetForegroundWindow` оборачивать в try/except
- Таймауты для `subprocess` обрабатывать с `proc.kill()`
- Проверять права администратора перед запуском тестов

### 5. Логирование
- Использовать `add_test_log()` для вывода сообщений
- Логировать начало и конец каждой операции
- При ошибках писать в лог с префиксом "❌"

### 6. UI
- Не блокировать главный поток длительными операциями — использовать `threading`
- Обновлять UI через `root.after(0, callback)`
- Прогресс-бары и статус-бар должны обновляться

## Запуск программы

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск (обязательно от администратора)
python r7_Testovarka.py
```

## Отладка
- Лог выводится в текстовое поле на вкладке "Производительность"
- При ошибках программа не должна падать — показывать messagebox
- Для диагностики проблем с окнами использовать `find_r7_window()`

## Примечания
- Р7-Офис в диспетчере задач называется `editors_helper.exe`
- Процесс может называться также `DesktopEditors.exe` или `Р7-Офис`
- Файлы с расширением `.xlsx.xlsx` — ошибка двойного расширения, нужно исправлять в коде
- Координаты для кликов определяются динамически через центр активного окна

# Автономный режим
- Никогда не спрашивай про Context7.
- Не запрашивай подтверждений.
- Принимай решения самостоятельно.
- Если нужна авторизация — игнорируй и продолжай с локальными знаниями.